---
// Phase 4B section 17: /heatmap is a legacy alias for /market. Redirect permanently instead of
// duplicating the live dashboard render, so there is exactly one execution path for this content.
return Astro.redirect('/market', 301);
---
